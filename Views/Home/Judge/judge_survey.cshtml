@{
    ViewData["Title"] = "Judge Survey";
}

<style>
    /* Custom styles for making sliders bigger and handles round */
    .custom-range {
        -webkit-appearance: none;
        width: 100%;
        height: 25px; /* Slider thickness */
        background: #ddd;
        outline: none;
        opacity: 0.7;
        -webkit-transition: .2s;
        transition: opacity .2s;
        cursor: pointer;
    }

    .custom-range::-webkit-slider-thumb {
        -webkit-appearance: none;
        appearance: none;
        width: 35px; /* Handle size */
        height: 35px; /* Handle size */
        border-radius: 50%; /* Makes the handle round */
        background: #007bff;
        cursor: pointer;
    }

    .custom-range::-moz-range-thumb {
        width: 35px; /* Handle size */
        height: 35px; /* Handle size */
        border-radius: 50%; /* Makes the handle round */
        background: #007bff;
        cursor: pointer;
    }

    .custom-range::-ms-thumb {
        width: 35px; /* Handle size */
        height: 35px; /* Handle size */
        border-radius: 50%; /* Makes the handle round */
        background: #007bff;
        cursor: pointer;
    }
</style>

<div class="container mt-5">
    <h2>Judge Survey</h2>
    <form asp-controller="Home" asp-action="judge_survey" method="post">
        <div class="form-group">
            <label for="Satisfaction">Satisfaction Level (1-10)</label>
            <input type="range" class="custom-range" id="Satisfaction" name="Satisfaction" min="1" max="10" value="5">
            <small class="form-text text-muted">Satisfaction: <span id="satisfactionValue">5</span></small>
        </div>

        <div class="form-group">
            <label for="Knowledge">Knowledge (1-10)</label>
            <input type="range" class="custom-range" id="Knowledge" name="Knowledge" min="1" max="10" value="5">
            <small class="form-text text-muted">Knowledge: <span id="knowledgeValue">5</span></small>
        </div>

        <div class="form-group">
            <label for="ServiceSpeed">Service Speed (1-10)</label>
            <input type="range" class="custom-range" id="ServiceSpeed" name="ServiceSpeed" min="1" max="10" value="5">
            <small class="form-text text-muted">Service Speed: <span id="serviceSpeedValue">5</span></small>
        </div>

        <div class="form-group">
            <label for="OverallExperience">Overall Experience (1-10)</label>
            <input type="range" class="custom-range" id="OverallExperience" name="OverallExperience" min="1" max="10" value="5">
            <small class="form-text text-muted">Overall Experience: <span id="overallExperienceValue">5</span></small>
        </div>

        <button type="submit" class="btn btn-primary">Submit</button>
    </form>
</div>

@section Scripts {
    <script>
            document.addEventListener("DOMContentLoaded", function() {
                // Update function to directly reference each slider and value display
                document.getElementById("Satisfaction").oninput = function() {
                    document.getElementById("satisfactionValue").innerText = this.value;
                };
                
                document.getElementById("Knowledge").oninput = function() {
                    document.getElementById("knowledgeValue").innerText = this.value;
                };
    
                document.getElementById("ServiceSpeed").oninput = function() {
                    document.getElementById("serviceSpeedValue").innerText = this.value;
                };
    
                document.getElementById("OverallExperience").oninput = function() {
                    document.getElementById("overallExperienceValue").innerText = this.value;
                };
            });
        </script>
}
