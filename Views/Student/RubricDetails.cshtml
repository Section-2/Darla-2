@{
    Layout = null; // Assuming you're using this page independently without a layout page

    //DONT THINK WE NEED THIS ANYMORE
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Rubric Details</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css">
    <script>
        // Example functionality to dynamically update the progress bar on the previous page
        // This would need to be adapted based on how you're managing state across pages
        function updateProgress() {
            // Logic to update progress bar based on checked items
            // This might involve storing progress in local storage, a database, or sending it back to the server
            console.log("Update progress based on checkboxes");
        }
    </script>
</head>
<body class="bg-blue-200 min-h-screen">
    <div class="container mx-auto p-4">
        <button onclick="window.history.back()" class="mb-4 bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">
            Back
        </button>
        <h1 class="text-3xl font-bold text-center mb-6">Rubric Details for IS 401</h1>
        <div class="bg-white p-6 rounded shadow">
            <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
                <div class="md:col-span-2">
                    <h2 class="text-xl font-semibold">Requirements</h2>
                    @for(int i = 0; i < 8; i++) { // Example loop, replace with your data
                        <div class="flex justify-between items-center mt-4">
                            <div class="text-lg">Requirement description goes here...</div>
                            <div class="text-lg font-bold">Points: 10</div>
                        </div>
                    }
                </div>
                <div>
                    <h2 class="text-xl font-semibold">Completed</h2>
                    @for(int i = 0; i < 8; i++) { // Example loop, replace with your data
                        <div class="flex items-center mt-4">
                            <input type="checkbox" class="transform scale-125" onchange="updateProgress()">
                            <label class="ml-2">Done</label>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</body>
</html>
